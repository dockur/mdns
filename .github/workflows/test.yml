on:
  push:
    branches-ignore:
      - master
    paths:
      - '**/*.sh'
      - 'Dockerfile'
      - '.github/workflows/check.yml'

  pull_request:
    paths:
      - '**/*.sh'
      - 'Dockerfile'
      - '.github/workflows/check.yml'

name: "Test"
permissions: {}

jobs:
  shellcheck:
    name: Test
    uses: ./.github/workflows/check.yml
